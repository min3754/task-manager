{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "MVP Task Manager for project and team collaboration",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "task-manager",
    "project-management",
    "collaboration"
  ],
  "author": "min3754",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
